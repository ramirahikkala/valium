# Production overrides — used on the server via:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  api:
    restart: unless-stopped
    networks:
      - default
      - db-network
      - caddy-proxy
    volumes:
      - plant_images:/app/plant_images

  web:
    restart: unless-stopped
    networks:
      - default
      - caddy-proxy

networks:
  caddy-proxy:
    external: true
  db-network:
    external: true
    name: db-network

volumes:
  plant_images:
