# Local development overrides — applied automatically by docker compose up
services:
  db:
    ports:
      - "5432:5432"

  api:
    ports:
      - "8000:8000"

  web:
    ports:
      - "3000:3000"
